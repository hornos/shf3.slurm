#!/bin/bash
source $(dirname ${BASH_SOURCE})/../../shf3/lib/header
include ${HOME}/shf3.slurm/lib
import que/slurm

nds="./${que_slurm_nodes}"
if test -r "${nds}" ; then
  rm -fv "${nds}"
fi
./${que_slurm_getnodes} "${nds}"
if failed $? ; then
  exit 1
fi
psshmgr slurm "${nds}" restart
